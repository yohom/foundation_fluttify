name: foundation_fluttify_example
description: Demonstrates how to use the foundation_fluttify plugin.
publish_to: 'none'

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  decorated_flutter: ^0.28.0-pre.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  foundation_fluttify:
    path: ../

flutter:
  uses-material-design: true